// This is a simple component that will be rendered at the top of the page.
// use command shift R to do this

const Header = () => {
  return (
    <header>
        <h1>Groceries List</h1>
    </header>
  )
}

export default Header